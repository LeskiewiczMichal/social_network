import { RegistrationForm } from '../features/authentication';

export default function Registration() {
  return (
    <div className="w-full flex justify-center p-3 dark:bg-gray-900">
      <RegistrationForm />
    </div>
  );
}
